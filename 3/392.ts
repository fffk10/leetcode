function isSubsequence(s: string, t: string): boolean {
  let result = true

  let S = s.split('')
  let T = t.split('')
  
  while (true) {
    
    if () {

    }
  }

  return result
}

console.log(isSubsequence('abc', 'ahbgdc'))
